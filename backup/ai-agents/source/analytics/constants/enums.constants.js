/** Mirrors the Prisma enums in schema.prisma — keep both in sync. */

const SCOPE_TYPE = Object.freeze({
  PLATFORM: "PLATFORM",
  COURSE: "COURSE",
  INSTRUCTOR: "INSTRUCTOR",
  STUDENT: "STUDENT",
});

/** Sentinel scopeId for the singleton PLATFORM scope — there's only ever one row per metric with scopeType=PLATFORM. */
const PLATFORM_SCOPE_ID = "platform";

const METRIC_KEY = Object.freeze({
  // Student
  STUDY_TIME: "STUDY_TIME",
  LEARNING_STREAK: "LEARNING_STREAK",
  PROGRESS_TREND: "PROGRESS_TREND",
  COMPLETION_RATE: "COMPLETION_RATE",
  ASSESSMENT_TREND: "ASSESSMENT_TREND",
  // Instructor
  COURSE_PERFORMANCE: "COURSE_PERFORMANCE",
  STUDENT_ENGAGEMENT: "STUDENT_ENGAGEMENT",
  TEACHING_EFFECTIVENESS: "TEACHING_EFFECTIVENESS",
  ASSESSMENT_QUALITY: "ASSESSMENT_QUALITY",
  // Course
  ENROLLMENT_TREND: "ENROLLMENT_TREND",
  COMPLETION_TREND: "COMPLETION_TREND",
  DROPOUT_RATE: "DROPOUT_RATE",
  AVERAGE_SCORE: "AVERAGE_SCORE",
  DIFFICULTY_SCORE: "DIFFICULTY_SCORE",
  // Platform
  ACTIVE_USERS: "ACTIVE_USERS",
  DAILY_ACTIVE_USERS: "DAILY_ACTIVE_USERS",
  MONTHLY_ACTIVE_USERS: "MONTHLY_ACTIVE_USERS",
  AI_USAGE: "AI_USAGE",
  FEATURE_USAGE: "FEATURE_USAGE",
  RETENTION: "RETENTION",
  CHURN: "CHURN",
  REVENUE_READY: "REVENUE_READY",
  SYSTEM_HEALTH: "SYSTEM_HEALTH",
});

const METRIC_KEYS_BY_SCOPE = Object.freeze({
  [SCOPE_TYPE.STUDENT]: [
    METRIC_KEY.STUDY_TIME,
    METRIC_KEY.LEARNING_STREAK,
    METRIC_KEY.PROGRESS_TREND,
    METRIC_KEY.COMPLETION_RATE,
    METRIC_KEY.ASSESSMENT_TREND,
  ],
  [SCOPE_TYPE.INSTRUCTOR]: [
    METRIC_KEY.COURSE_PERFORMANCE,
    METRIC_KEY.STUDENT_ENGAGEMENT,
    METRIC_KEY.TEACHING_EFFECTIVENESS,
    METRIC_KEY.ASSESSMENT_QUALITY,
  ],
  [SCOPE_TYPE.COURSE]: [
    METRIC_KEY.ENROLLMENT_TREND,
    METRIC_KEY.COMPLETION_TREND,
    METRIC_KEY.DROPOUT_RATE,
    METRIC_KEY.AVERAGE_SCORE,
    METRIC_KEY.DIFFICULTY_SCORE,
  ],
  [SCOPE_TYPE.PLATFORM]: [
    METRIC_KEY.ACTIVE_USERS,
    METRIC_KEY.DAILY_ACTIVE_USERS,
    METRIC_KEY.MONTHLY_ACTIVE_USERS,
    METRIC_KEY.AI_USAGE,
    METRIC_KEY.FEATURE_USAGE,
    METRIC_KEY.RETENTION,
    METRIC_KEY.CHURN,
    METRIC_KEY.REVENUE_READY,
    METRIC_KEY.SYSTEM_HEALTH,
  ],
});

const TREND_DIRECTION = Object.freeze({
  UP: "UP",
  DOWN: "DOWN",
  STABLE: "STABLE",
});

const REPORT_TYPE = Object.freeze({
  WEEKLY: "WEEKLY",
  MONTHLY: "MONTHLY",
  QUARTERLY: "QUARTERLY",
  ANNUAL: "ANNUAL",
});

const EXPORT_FORMAT = Object.freeze({
  JSON: "JSON",
  CSV: "CSV",
});

module.exports = {
  SCOPE_TYPE,
  PLATFORM_SCOPE_ID,
  METRIC_KEY,
  METRIC_KEYS_BY_SCOPE,
  TREND_DIRECTION,
  REPORT_TYPE,
  EXPORT_FORMAT,
};
