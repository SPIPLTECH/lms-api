/** Mirrors the Prisma enums in schema.prisma — keep both in sync. */

const SCOPE_TYPE = Object.freeze({
  PLATFORM: "PLATFORM",
  DEPARTMENT: "DEPARTMENT",
  FACULTY: "FACULTY",
  COURSE: "COURSE",
  STUDENT: "STUDENT",
});

/** Sentinel scopeId for the singleton PLATFORM scope — same convention as Analytics' PLATFORM_SCOPE_ID. */
const PLATFORM_SCOPE_ID = "platform";

/** Course.category is nullable free text — this is the departmentKey used when a course has none, so every course still lands in exactly one department bucket. */
const UNCATEGORIZED_DEPARTMENT_KEY = "UNCATEGORIZED";

const INSIGHT_CATEGORY = Object.freeze({
  ACADEMIC: "ACADEMIC",
  OPERATIONAL: "OPERATIONAL",
  FINANCIAL: "FINANCIAL",
  ENGAGEMENT: "ENGAGEMENT",
  AI_ADOPTION: "AI_ADOPTION",
  RISK: "RISK",
});

const INSIGHT_STATUS = Object.freeze({
  ACTIVE: "ACTIVE",
  EXPIRED: "EXPIRED",
});

const RECOMMENDATION_TYPE = Object.freeze({
  COURSE_IMPROVEMENT: "COURSE_IMPROVEMENT",
  STAFFING_CHANGE: "STAFFING_CHANGE",
  CURRICULUM_UPDATE: "CURRICULUM_UPDATE",
  RESOURCE_ALLOCATION: "RESOURCE_ALLOCATION",
  RISK_INTERVENTION: "RISK_INTERVENTION",
});

const RECOMMENDATION_STATUS = Object.freeze({
  ACTIVE: "ACTIVE",
  EXPIRED: "EXPIRED",
});

const COMPLIANCE_CHECK_TYPE = Object.freeze({
  CERTIFICATE_INTEGRITY: "CERTIFICATE_INTEGRITY",
  COURSE_PUBLISHING_HYGIENE: "COURSE_PUBLISHING_HYGIENE",
  AI_DECISION_QUALITY: "AI_DECISION_QUALITY",
  DATA_COMPLETENESS: "DATA_COMPLETENESS",
});

const COMPLIANCE_OUTCOME = Object.freeze({
  PASS: "PASS",
  FAIL: "FAIL",
});

const COMPLIANCE_SEVERITY = Object.freeze({
  INFO: "INFO",
  WARNING: "WARNING",
  CRITICAL: "CRITICAL",
});

const CAPACITY_RESOURCE_TYPE = Object.freeze({
  STUDENT_ENROLLMENT: "STUDENT_ENROLLMENT",
  INSTRUCTOR_CAPACITY: "INSTRUCTOR_CAPACITY",
  COURSE_CAPACITY: "COURSE_CAPACITY",
  INFRASTRUCTURE_LOAD: "INFRASTRUCTURE_LOAD",
});

const FORECAST_METHOD = Object.freeze({
  LINEAR_REGRESSION: "LINEAR_REGRESSION",
  RATIO_PROJECTION: "RATIO_PROJECTION",
  TREND_EXTRAPOLATION: "TREND_EXTRAPOLATION",
});

const REPORT_TYPE = Object.freeze({
  WEEKLY: "WEEKLY",
  MONTHLY: "MONTHLY",
  QUARTERLY: "QUARTERLY",
  ANNUAL: "ANNUAL",
  SEMESTER: "SEMESTER",
});

const GOVERNANCE_METRIC_KEY = Object.freeze({
  POLICY_COMPLIANCE_RATE: "POLICY_COMPLIANCE_RATE",
  CERTIFICATE_INTEGRITY_RATE: "CERTIFICATE_INTEGRITY_RATE",
  DATA_COMPLETENESS_RATE: "DATA_COMPLETENESS_RATE",
  AI_DECISION_AUDIT_PASS_RATE: "AI_DECISION_AUDIT_PASS_RATE",
  ACCREDITATION_READINESS: "ACCREDITATION_READINESS",
});

const ALERT_TYPE = Object.freeze({
  INSTRUCTOR_OVERLOAD: "INSTRUCTOR_OVERLOAD",
  INACTIVE_INSTRUCTOR: "INACTIVE_INSTRUCTOR",
  DEPARTMENT_DECLINE: "DEPARTMENT_DECLINE",
  COURSE_DECLINE: "COURSE_DECLINE",
  COMPLIANCE_FAILURE: "COMPLIANCE_FAILURE",
  HIGH_RISK_SURGE: "HIGH_RISK_SURGE",
  CAPACITY_WARNING: "CAPACITY_WARNING",
});

/** Shared with Teacher Insight's StudentAlert/CourseInsight/TeachingRecommendation — same lifecycle (ACTIVE until the underlying condition clears, then auto-flips to RESOLVED). */
const ALERT_STATUS = Object.freeze({
  ACTIVE: "ACTIVE",
  RESOLVED: "RESOLVED",
});

const ALERT_PRIORITY = Object.freeze({
  LOW: "LOW",
  MEDIUM: "MEDIUM",
  HIGH: "HIGH",
});

const EXPORT_FORMAT = Object.freeze({
  JSON: "JSON",
  CSV: "CSV",
});

module.exports = {
  SCOPE_TYPE,
  PLATFORM_SCOPE_ID,
  UNCATEGORIZED_DEPARTMENT_KEY,
  INSIGHT_CATEGORY,
  INSIGHT_STATUS,
  RECOMMENDATION_TYPE,
  RECOMMENDATION_STATUS,
  COMPLIANCE_CHECK_TYPE,
  COMPLIANCE_OUTCOME,
  COMPLIANCE_SEVERITY,
  CAPACITY_RESOURCE_TYPE,
  FORECAST_METHOD,
  REPORT_TYPE,
  GOVERNANCE_METRIC_KEY,
  ALERT_TYPE,
  ALERT_STATUS,
  ALERT_PRIORITY,
  EXPORT_FORMAT,
};
