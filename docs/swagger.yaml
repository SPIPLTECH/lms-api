openapi: 3.0.0

info:
  title: LMS API
  version: 1.0.0

paths:

  /api/auth/register:
    post:
      summary: Register User

  /api/auth/login:
    post:
      summary: Login User

  /api/courses:
    get:
      summary: Get Courses

    post:
      summary: Create Course