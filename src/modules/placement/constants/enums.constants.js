/** Mirrors the Prisma enums in schema.prisma — keep both in sync. */

const OPPORTUNITY_TYPE = Object.freeze({ JOB: "JOB", INTERNSHIP: "INTERNSHIP" });

const EMPLOYMENT_TYPE = Object.freeze({
  FULL_TIME: "FULL_TIME",
  PART_TIME: "PART_TIME",
  CONTRACT: "CONTRACT",
  FREELANCE: "FREELANCE",
});

const OPPORTUNITY_STATUS = Object.freeze({ OPEN: "OPEN", CLOSED: "CLOSED", FILLED: "FILLED" });

const OPPORTUNITY_SOURCE = Object.freeze({ INTERNAL: "INTERNAL", EXTERNAL_API: "EXTERNAL_API" });

const JOB_MATCH_PRIORITY = Object.freeze({ LOW: "LOW", MEDIUM: "MEDIUM", HIGH: "HIGH" });

const JOB_MATCH_STATUS = Object.freeze({ ACTIVE: "ACTIVE", APPLIED: "APPLIED", DISMISSED: "DISMISSED", EXPIRED: "EXPIRED" });

const APPLICATION_STATUS = Object.freeze({
  APPLIED: "APPLIED",
  UNDER_REVIEW: "UNDER_REVIEW",
  SHORTLISTED: "SHORTLISTED",
  INTERVIEWING: "INTERVIEWING",
  OFFERED: "OFFERED",
  REJECTED: "REJECTED",
  WITHDRAWN: "WITHDRAWN",
  ACCEPTED: "ACCEPTED",
});

const INTERVIEW_TYPE = Object.freeze({
  TECHNICAL: "TECHNICAL",
  HR: "HR",
  MANAGERIAL: "MANAGERIAL",
  APTITUDE: "APTITUDE",
  CODING: "CODING",
});

const INTERVIEW_STATUS = Object.freeze({ SCHEDULED: "SCHEDULED", COMPLETED: "COMPLETED", CANCELLED: "CANCELLED", RESCHEDULED: "RESCHEDULED" });

const INTERVIEW_OUTCOME = Object.freeze({ PENDING: "PENDING", PASSED: "PASSED", FAILED: "FAILED" });

const OFFER_STATUS = Object.freeze({ PENDING: "PENDING", ACCEPTED: "ACCEPTED", DECLINED: "DECLINED", EXPIRED: "EXPIRED" });

const DRIVE_STATUS = Object.freeze({ UPCOMING: "UPCOMING", ONGOING: "ONGOING", COMPLETED: "COMPLETED", CANCELLED: "CANCELLED" });

/** PlacementProfile.preparationSuggestions entry types — no dedicated recommendation-ledger model exists for these in this spec's Database Design, so they're computed/embedded rather than persisted as their own rows. */
const PREPARATION_SUGGESTION_TYPE = Object.freeze({
  MOCK_INTERVIEW: "MOCK_INTERVIEW",
  CODING_ASSESSMENT: "CODING_ASSESSMENT",
  RESUME_IMPROVEMENT: "RESUME_IMPROVEMENT",
  PORTFOLIO_IMPROVEMENT: "PORTFOLIO_IMPROVEMENT",
  COMPANY_SPECIFIC_PREP: "COMPANY_SPECIFIC_PREP",
});

module.exports = {
  OPPORTUNITY_TYPE,
  EMPLOYMENT_TYPE,
  OPPORTUNITY_STATUS,
  OPPORTUNITY_SOURCE,
  JOB_MATCH_PRIORITY,
  JOB_MATCH_STATUS,
  APPLICATION_STATUS,
  INTERVIEW_TYPE,
  INTERVIEW_STATUS,
  INTERVIEW_OUTCOME,
  OFFER_STATUS,
  DRIVE_STATUS,
  PREPARATION_SUGGESTION_TYPE,
};
