/** Mirrors the Prisma enums in schema.prisma — keep both in sync. */

const ALERT_TYPE = Object.freeze({
  AT_RISK: "AT_RISK",
  STRUGGLING: "STRUGGLING",
  INACTIVE: "INACTIVE",
  TOP_PERFORMER: "TOP_PERFORMER",
});

const COURSE_INSIGHT_TYPE = Object.freeze({
  WEAK_CONCEPT: "WEAK_CONCEPT",
  STRONG_CONCEPT: "STRONG_CONCEPT",
  LESSON_COMPLETION_TREND: "LESSON_COMPLETION_TREND",
  QUIZ_PERFORMANCE_TREND: "QUIZ_PERFORMANCE_TREND",
  ASSIGNMENT_PERFORMANCE_TREND: "ASSIGNMENT_PERFORMANCE_TREND",
});

const TEACHING_RECOMMENDATION_TYPE = Object.freeze({
  INSTRUCTOR_ACTION: "INSTRUCTOR_ACTION",
  TEACHING_SUGGESTION: "TEACHING_SUGGESTION",
});

const TEACHER_REPORT_TYPE = Object.freeze({
  WEEKLY_SUMMARY: "WEEKLY_SUMMARY",
  MONTHLY_SUMMARY: "MONTHLY_SUMMARY",
});

const INSIGHT_PRIORITY = Object.freeze({
  LOW: "LOW",
  MEDIUM: "MEDIUM",
  HIGH: "HIGH",
});

const INSIGHT_STATUS = Object.freeze({
  ACTIVE: "ACTIVE",
  RESOLVED: "RESOLVED",
});

const INSIGHT_SOURCE_MODEL = Object.freeze({
  STUDENT_ALERT: "STUDENT_ALERT",
  COURSE_INSIGHT: "COURSE_INSIGHT",
  TEACHING_RECOMMENDATION: "TEACHING_RECOMMENDATION",
});

const RETIRED_REASON = Object.freeze({
  SUPERSEDED: "SUPERSEDED",
});

module.exports = {
  ALERT_TYPE,
  COURSE_INSIGHT_TYPE,
  TEACHING_RECOMMENDATION_TYPE,
  TEACHER_REPORT_TYPE,
  INSIGHT_PRIORITY,
  INSIGHT_STATUS,
  INSIGHT_SOURCE_MODEL,
  RETIRED_REASON,
};
