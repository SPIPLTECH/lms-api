/** Mirrors the Prisma enums in schema.prisma — keep both in sync. */

const DIFFICULTY_ADJUSTMENT = Object.freeze({ EASE_UP: "EASE_UP", STANDARD: "STANDARD", ACCELERATE: "ACCELERATE" });

const RECOMMENDATION_TYPE = Object.freeze({
  NEXT_LESSON: "NEXT_LESSON",
  NEXT_MODULE: "NEXT_MODULE",
  REVISION: "REVISION",
  PREREQUISITE: "PREREQUISITE",
  PACE_ADJUSTMENT: "PACE_ADJUSTMENT",
});

const RECOMMENDATION_PRIORITY = Object.freeze({ LOW: "LOW", MEDIUM: "MEDIUM", HIGH: "HIGH" });

const RECOMMENDATION_STATUS = Object.freeze({ ACTIVE: "ACTIVE", COMPLETED: "COMPLETED", DISMISSED: "DISMISSED", EXPIRED: "EXPIRED" });

/** Fixed priority per type — there's no competing-candidates ranking engine here, just a small well-defined set. */
const PRIORITY_BY_RECOMMENDATION_TYPE = Object.freeze({
  [RECOMMENDATION_TYPE.NEXT_LESSON]: RECOMMENDATION_PRIORITY.HIGH,
  [RECOMMENDATION_TYPE.PREREQUISITE]: RECOMMENDATION_PRIORITY.HIGH,
  [RECOMMENDATION_TYPE.NEXT_MODULE]: RECOMMENDATION_PRIORITY.MEDIUM,
  [RECOMMENDATION_TYPE.REVISION]: RECOMMENDATION_PRIORITY.MEDIUM,
  [RECOMMENDATION_TYPE.PACE_ADJUSTMENT]: RECOMMENDATION_PRIORITY.LOW,
});

const STUDY_PLAN_TYPE = Object.freeze({ DAILY: "DAILY", WEEKLY: "WEEKLY" });

const MILESTONE_TYPE = Object.freeze({ MODULE_COMPLETION: "MODULE_COMPLETION", COURSE_COMPLETION: "COURSE_COMPLETION" });

const MILESTONE_STATUS = Object.freeze({ PENDING: "PENDING", ACHIEVED: "ACHIEVED" });

const REVISION_PLAN_STATUS = Object.freeze({ PENDING: "PENDING", COMPLETED: "COMPLETED", SKIPPED: "SKIPPED" });

const LEARNING_GOAL_TYPE = Object.freeze({ COURSE_COMPLETION: "COURSE_COMPLETION", TOPIC_MASTERY: "TOPIC_MASTERY", CUSTOM: "CUSTOM" });

const LEARNING_GOAL_STATUS = Object.freeze({ ACTIVE: "ACTIVE", ACHIEVED: "ACHIEVED", ABANDONED: "ABANDONED" });

/** Mirrors Student State's LEARNING_SPEED values on StudentBehavior.preferredLearningSpeed. */
const LEARNING_SPEED = Object.freeze({ SLOW: "SLOW", NORMAL: "NORMAL", FAST: "FAST" });

module.exports = {
  DIFFICULTY_ADJUSTMENT,
  RECOMMENDATION_TYPE,
  RECOMMENDATION_PRIORITY,
  RECOMMENDATION_STATUS,
  PRIORITY_BY_RECOMMENDATION_TYPE,
  STUDY_PLAN_TYPE,
  MILESTONE_TYPE,
  MILESTONE_STATUS,
  REVISION_PLAN_STATUS,
  LEARNING_GOAL_TYPE,
  LEARNING_GOAL_STATUS,
  LEARNING_SPEED,
};
